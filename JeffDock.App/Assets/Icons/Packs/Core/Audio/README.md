# Core Audio Icons

Add these square, unrotated image files here (`.png`, `.jpg`, or `.jpeg`):

- `toggle-mute`
- `toggle-microphone-mute`
- `volume-up`
- `volume-down`

Their icon IDs will be `core/audio/<filename>`.
