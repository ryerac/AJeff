<Window x:Class="JeffDock.App.SceneNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Scene"
        Width="360"
        Height="150"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Scene name" Margin="0,0,0,6" />
        <TextBox x:Name="SceneNameTextBox"
                 Grid.Row="1"
                 MaxLength="80" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="Cancel" Width="76" Margin="0,0,8,0" IsCancel="True" />
            <Button x:Name="SubmitButton" Content="Create" Width="76" IsDefault="True" Click="CreateButton_OnClick" />
        </StackPanel>
    </Grid>
</Window>
